pipeline {
    agent {
        label 'static-agent'
    }

    stages {
        stage('Build') {
            steps {
                echo 'build in progress...'
            }
        }

        stage('Test') {
            steps {
                echo 'test in progress...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'deploy in progress.00..'
            }
        }
    }
}
